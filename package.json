{
  "name": "pureable-objects",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "spago build && node apps/site/scripts/css.mjs",
    "bundle": "spago bundle -p chat && spago bundle -p counter && spago bundle -p ui",
    "config": "node apps/site/scripts/config.mjs",
    "dev": "npm run build && npm run bundle && npm run config && wrangler dev --config apps/site/wrangler.jsonc",
    "deploy": "npm run build && npm run bundle && npm run config && wrangler deploy --config apps/site/wrangler.jsonc",
    "test": "spago test",
    "test:e2e": "playwright test -c apps/site/playwright.config.ts"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.62.1",
    "wrangler": "^4.125.0"
  }
}
